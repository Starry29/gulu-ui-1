<template>
    <p>
      通过设置 <code>arrow</code> 属性为 <code>default</code>，来设置切换箭头只有在鼠标移动到走马灯上时才会显示，离开就不显示。 
    </p>
    <p>
      通过设置 <code>arrow</code> 属性为 <code>always</code>，来设置切换箭头一直显示。
    </p>
    <p>
      默认情况下，将设置 <code>arrow</code> 属性为 <code>never</code>，则箭头会一直隐藏。
    </p>
</template>
